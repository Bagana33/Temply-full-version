# Supabase Service Role Key Засах Заавар

## Асуудал
"Серверийн тохиргоо буруу байна. Supabase API keys-ийг шалгана уу." гэсэн алдаа гарч байна.

## Шийдэл

### 1. Supabase Dashboard-аас Service Role Key авна уу

1. https://app.supabase.com руу очно уу
2. Таны project-ийг сонгоно уу (hrtaescazsisyeftbdln)
3. **Settings** → **API** руу очно уу
4. **Project API keys** хэсэгт:
   - **service_role** key-ийг олоно уу (anon key биш!)
   - **"Reveal"** эсвэл **"Copy"** товч дараана уу
   - Бүх key-ийг хуулна уу (эхлээд `eyJ...` гэж эхэлнэ)

### 2. .env.local файл засах

1. `.env.local` файлыг нээнэ уу
2. `SUPABASE_SERVICE_ROLE_KEY=` гэсэн мөрийг олоно уу
3. Хуучин key-ийг устгаад, шинэ service_role key-ийг оруулна уу:

```bash
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImhydGFlc2NhenNpc3llZnRiZGxuIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2NDQzODgzNiwiZXhwIjoyMDgwMDE0ODM2fQ.XXXXX
```

**Анхаар:**
- Key нь `service_role` байх ёстой, `anon` биш
- Key нь 3 хэсгээс бүрдэнэ (`.` тэмдгээр тусгаарлагдсан)
- Бүх key-ийг хуулна уу (маш урт байна)

### 3. Development Server дахин эхлүүлэх

Terminal дээр:
1. Server зогсоох: `Ctrl + C` (Mac дээр `Cmd + C`)
2. Дахин эхлүүлэх:
   ```bash
   npm run dev
   ```

### 4. Шалгах

1. http://localhost:3001/auth/register руу очно уу
2. Бүртгэл үүсгэх оролдлого хийж үзнэ
3. Одоо алдаа гарахгүй байх ёстой

## Хэрэв алдаа одоо ч гарвал

1. **Terminal log шалгах:**
   - Terminal дээрх алдааны мессежийг уншина уу
   - "Supabase registration error:" гэсэн мөр олоно уу

2. **Supabase Dashboard шалгах:**
   - Settings → API → Project API keys
   - service_role key зөв эсэхийг шалгана уу
   - Key-ийг дахин generate хийж болно (Reveal → Reset)

3. **.env.local файл шалгах:**
   - Key-ийн урт зөв эсэхийг шалгана уу
   - Key-ийн эхэнд `eyJ` байх ёстой
   - Key-ийн төгсгөлд `=` эсвэл space байхгүй байх ёстой

Амжилт хүсье! 🚀

