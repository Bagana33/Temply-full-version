# Database Schema Суулгах Заавар (Монгол)

## Хамгийн Хялбар Арга: Supabase Dashboard

### Алхам 1: Supabase Dashboard нээх
1. https://app.supabase.com руу очно уу
2. Нэвтрэх (login хийх)
3. Таны project-ийг сонгоно уу

### Алхам 2: SQL Editor нээх
1. Зүүн талын цэснээс "SQL Editor" сонгоно уу
2. Шинэ query tab нээгдэнэ
3. "New query" товч дараана уу

### Алхам 3: Schema файлыг хуулах
1. Энэ project-ийн `supabase-schema.sql` файлыг нээнэ уу
2. Бүх агуулгыг сонгож хуулна (Command+A, Command+C)
3. Supabase Dashboard-ийн SQL Editor-д буулгана (Command+V)

### Алхам 4: Ажиллуулах
1. "Run" эсвэл "Execute" товч дараана уу (эсвэл Command+Enter)
2. Хүлээх (хэдэн секунд үргэлжилнэ)
3. Амжилттай бол "Success" мессеж харагдана

### Алхам 5: Шалгах
Дараах query ажиллуулж хүснэгтүүд үүссэн эсэхийг шалгана:

```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
ORDER BY table_name;
```

Харагдах ёстой хүснэгтүүд:
- users
- templates
- purchases
- downloads
- membership
- cart_items
- payouts
- reviews
- posts
- profiles
- follows

## Асуудал гарвал

Хэрэв алдаа гарвал:
1. SQL Editor-д алдааны мессежийг уншина уу
2. Schema файл дотор "already exists" гэсэн алдаа гарвал энэ нь хэвийн (idempotent байдаг)
3. Хэрэв бусад алдаа гарвал, алдааны мөрний дугаарыг шалгана уу

## Дараагийн Алхам

Schema амжилттай суусны дараа:
- Supabase Dashboard-аас хүснэгтүүдийг шалгана уу
- Row Level Security policies зөв тохируулагдсан эсэхийг шалгана уу
- Environment variables тохируулаарай:
  - `NEXT_PUBLIC_SUPABASE_URL`
  - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
  - `SUPABASE_SERVICE_ROLE_KEY`

Амжилт хүсье! 🚀
